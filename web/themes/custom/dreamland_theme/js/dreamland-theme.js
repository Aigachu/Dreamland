/**
 * @file
 * Dreamland Theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.dreamlandTheme = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
